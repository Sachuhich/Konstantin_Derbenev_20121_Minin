# число десятков и единиц в 2значном числе
# сумма цифр 2знач числа
# число при перестановке цифр в 2знач числе
# 3знач число по цифрам через ,

# Считывает 1 строку if 'python', ДА иначе НЕТ
# Определяет, состоит ли 2знач число, введён с клавы, из одинак цифр и если состоти, то ДА иначе НЕТ
# Считывает 3 числа и подсчит кол-во чёт цифр
# Является ли заданное натуральное число 3значным
# Проверяет, что все цифры зад натрульного 3знач числа различны
# По координатам точки, не лежащим на осях координат определяет номер координатной четверти, в которой она находится
# Даны 3 целых числа. Определить, сколько среди них совпадают. Вывод: ВСЕ СОВПАДАЮТ, 2 СОВП, НИКАКОЕ НЕ СОВПАДАЕТ

'''
num=int(input())                # Блок 1
if len(str(num))==2:
    a=int(str(num)[0])
    b=int(str(num)[1])
    sum_num=a+b
    num2=int(str(b)+str(a))
    print('Кол-во десятков: ',a,'\n','Кол-во единиц: ',b,sep=(''))                  # число десятков и единиц в 2значном числе
    print('Сумма цифр:',sum_num)                                                    # сумма цифр 2знач числа
    print('Число после перестановки цифр:',num2)                                    # число при перестановке цифр в 2знач числе
if len(str(num))==3:
    print(str(num)[0],str(num)[1],str(num)[2],sep=(','))                            # 3знач число по цифрам через ,
'''

'''
strin=input()                     # Блок 2
print('ДА') if strin=='Python' else print('НЕТ')                                    # Считывает 1 строку if 'python', ДА иначе НЕТ
'''
'''
num=int(input())
print('ДА') if int(str(num)[0])==int(str(num)[1]) else print('НЕТ')                 # Определяет, состоит ли 2знач число, введён с клавы, из одинак цифр и если состоти, то ДА иначе НЕТ
'''
'''
num1,num2,num3,d,c=int(input()),int(input()),int(input()),[n for n in range(0,10,2)],0
num0=str(num1)+str(num2)+str(num3)
for i in range(len(num0)):
    if num0[i]=='0'or num0[i]=='2'or num0[i]=='4'or num0[i]=='6'or num0[i]=='8':c+=1
print(c)                                                                            # Считывает 3 числа и подсчит кол-во чёт цифр
'''